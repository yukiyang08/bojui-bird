# 不揪鳥 Bu-Jiu Bird

LINE 群組記點排行榜與聚餐算帳機器人

## 這是什麼

一個放在 LINE 群組裡的機器人。平常大家互相記點數，可以是懲罰也可以是嘉獎。聚餐結帳時，點數會自動變成每個人要多付或少付的錢。

## 三個主要功能

**1. 記點**

在群組裡輸入指令幫人加分或扣分，附上原因。

- 扣分（做錯事）：`+1 @小明 遲到`
- 加分（做好事）：`-1 @老王 買手搖`

**2. 排行榜**

輸入 `!排行榜`，看誰點數最高最低。

- 功德榜：點數最低的人（做最多好事）
- 罪人榜：點數最高的人（做最多壞事）

輸入 `!查 @小明` 可以看某人最近五筆紀錄。

**3. 聚餐算帳**

輸入 `!算帳 11880`，機器人自動算出每個人該付多少。

算法：
- 先算出每人平均要付多少錢
- 每一點加分或扣分，對應調整一筆固定金額（例如一點 100 元）
- 罪人多付一點，功德多的人少付一點
- 最後校正，確保大家付的錢加起來剛好等於總金額

## 資料庫

三張表就夠：

- **groups**：存每個群組的設定，例如一點折抵多少錢
- **group_members**：存每個人目前的總點數
- **point_records**：存每一筆記點的歷史，誰記的、記誰的、原因是什麼

## 技術架構

LINE 群組 → 機器人後端（Python FastAPI）→ 資料庫（Supabase）

後端放在 Render 或 Zeabur，接 GitHub 自動部署。

需要三個環境變數：LINE 頻道密鑰、LINE 存取權杖、資料庫連線字串。