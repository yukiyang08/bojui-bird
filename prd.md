# 不揪鳥 Bu-Jiu Bird

LINE 群組記點排行榜與聚餐算帳機器人

## 這是什麼

一個放在 LINE 群組裡的機器人。平常大家互相記點數，做好事加分、做錯事扣分。聚餐結帳時，點數會自動變成每個人要多付或少付的錢。

## 三個主要功能

**1. 記點**

在群組裡輸入指令幫人加分或扣分，附上原因。

- 扣分（做錯事）：`-1 @小明 遲到`
- 加分（做好事）：`+1 @老王 買手搖`

不能記給不揪鳥自己。`+1 @不揪鳥` 之類它會回：「我只負責記帳 不負責付錢喔 不揪不揪」。

單純 @不揪鳥（沒帶記點指令）或輸入 `!說明`，它會回完整使用說明。

**2. 排行榜**

輸入 `!排行榜`，看誰點數最高最低。

- 功德榜：點數最高的人（做最多好事）
- 罪人榜：點數最低的人（做最多壞事）

輸入 `!查 @小明` 可以看某人最近五筆紀錄。

LIFF 排行榜頁（`/liff/liff.html`）顯示總排名（分數高到低）＋ 功德紀錄／犯罪紀錄兩個活動流。可以直接在頁面上點紀錄修改事蹟或點數、刪除紀錄，也可以「手動記一筆」給名單上的人。

**3. 聚餐算帳**

輸入 `!算帳 11880`，機器人自動算出每個人該付多少。

先用 `!目標 11880` 設好大餐目標金額，之後直接打 `!算帳`（不帶數字）就用這個金額。目標金額也可以在 LIFF 排行榜頁上直接點「大餐目標」卡片設定。

算法（依分數比例分攤，不會有人付負的 / 退錢）：
- 每個人的分攤權重 = (全場最高分 − 我的分數) + 1
- 分數越高權重越小、付越少；分數最低的人付最多；大家同分就均分
- 依權重把總金額分下去，四捨五入的零頭讓目前付最多的人吸收
- 加起來剛好等於總金額，而且每個人都 ≥ 0

## 資料庫

三張表就夠：

- **groups**：存每個群組的設定，大餐目標金額（dinner_target_twd）
- **group_members**：存每個人目前的總點數
- **point_records**：存每一筆記點的歷史，誰記的、記誰的、原因是什麼
- **dinner_events**：存每次算帳的名稱與總金額

## 技術架構

LINE 群組 → 機器人後端（Python FastAPI）→ 資料庫（Supabase）

後端放在 Render 或 Zeabur，接 GitHub 自動部署。

需要三個環境變數：LINE 頻道密鑰、LINE 存取權杖、資料庫連線字串。